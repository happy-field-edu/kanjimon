# 📝 手書きノート → 漢字4択クイズ 変換パイプライン 引き継ぎ資料

先生の手書きノート（写真PDF）から、ゲームで遊べる4択クイズデータ（画像＋CSV）を作る手順書。
**2026年 2年生 1学期ぶん（40問）を実際にこの手順で作成・公開済み。** 次の学期も同じ手順で作れる。

---

## 0. 成果物（1学期ぶん・完成済み）

| もの | 場所 |
|---|---|
| クイズ画像 160枚（40問×4択） | `images/quiz/q{ページ}{列2桁}_{行}.png` |
| クイズCSV 40問 | `questions_grade2_term1.csv` |
| 公開CSV URL（ゲームに貼る） | `https://happy-field-edu.github.io/kanjimon/questions_grade2_term1.csv` |
| 元PDF | `2026_2年_1学期_漢字４択クイズ.pdf` |

**遊び方**：https://happy-field-edu.github.io/kanjimon/ を開き、最初の画面のURL欄に上記CSV URLを貼って「ぼうけんに しゅっぱつ！」。

---

## 1. 元データ（ノート）の書き方ルール ★重要

このパイプラインは、ノートが以下の形式で書かれている前提。**次回もこの形式で書くこと。**

- **1列＝1問**。縦に4つ、同じ漢字のバリエーションを書く。
- **上から4つ目（4行目）が正解**。1〜3行目は「子どもがやりがちな、実在しない誤字」。
- 1ページあたり **8列 × 4行 = 8問**。
- マス目ノート（水色の罫線）でOK。**下敷きなし・多少の傾き・黒ずみは自動で補正するので気にしなくてよい。**

> 誤字の作り方の例（1学期ぶんで実際に使われたもの）
> - 突き抜ける／突き抜けない（例：黄、書）
> - 点の有無・位置（例：太 vs 大 vs 犬）
> - 画数の増減（例：曜、聞）
> - 部品の形違い（例：図、絵）

---

## 2. 変換の全手順（Claudeが自動実行）

ユーザーはPDFを渡すだけ。以下はClaude側の処理内容。

### Step 1. PDF → 画像
```bash
pdftoppm -r 200 -png "<PDF>" /tmp/kanji_src/p
```
※ 5ページで約1〜2分かかる（バックグラウンド実行推奨）。

### Step 2. 傾き補正（deskew）
- 水色の罫線を色で抽出：`(B - R > 15) & (B > 110)`
- −2.5°〜+2.5°を0.1°刻みで回転し、**行方向の投影分散が最大**になる角度を採用
- 1学期の実績：ページごとに −1.0° 〜 +2.1° とバラバラだった（1枚ずつ判定が必要）

### Step 3. クリーンアップ（マス線・黒ずみ除去＋濃さ均一化）
- **マス線除去**：水色マスクの画素を白へ
- **鉛筆抽出**：`輝度 < 175` かつ `彩度 < 75`（＝暗いグレー）だけ残す
- **黒ずみ・影除去**：上記条件外は白に飛ばす
- **濃さ均一化**：行ごとにインク量の85パーセンタイルで正規化（薄い行を他と同じ濃さへ）

### Step 4. 行・列の検出
- 行：インク量の横方向プロファイルから4バンドを検出。**写り込んだ指などは高さ・位置で除外**
- 列：**行ごとに連結成分で8文字を個別検出**する
  - ⚠️ ページ全体で列を切ると失敗する。行ごとに文字の左右位置がズレているため（1学期ページ2で列ズレ事故が発生）
  - 偏と旁が離れる字（例：行）は分裂するので、**統合ギャップを18→68pxまで広げながら、ちょうど8個になる値を採用**

### Step 5. 1文字ずつ切り出し
- 各セルで **細い直線だけを除去**（マス線の残り対策）
  - 判定：長さが辺の50%超 かつ 周辺3pxのインクが薄い（≒細い）→ 直線とみなして消す
  - 字の画は太いので残る。**160枚中、字が欠けた画像は0枚**（旧版とインク量比較で検証済み）
- バウンディングボックスでトリム → 余白1.2倍の正方形に配置 → **256×256px・白背景PNG**

### Step 6. ファイル命名
```
images/quiz/q{ページ}{列2桁}_{行}.png
例) q304_4.png = 3ページ目・4列目・4行目（＝この問題の正解）
```

### Step 7. CSV生成
ゲームのCSV仕様（`問題文, 正解画像URL, 間違い画像URL1〜3`）に合わせて出力。
- 正解＝`_4.png`、誤答＝`_1〜3.png`
- URLは `https://happy-field-edu.github.io/kanjimon/images/quiz/...`

---

## 3. 出題文の方針（A案：読みから選ばせる）★確定仕様

```
「{読み}」の かんじは どれ？
```

**なぜ読み方式か**：「いま」の漢字を「会」だと思って選ぶ子がいる。読みから正しい字を選ばせることで、**形の細部だけでなく「意味と字の対応」も同時に確認**できる。

読みは **2年生に馴染む語** を選ぶ（1学期ぶんで実際に調整した例）：

| 漢字 | ✕ 避けた読み | ○ 採用した読み |
|---|---|---|
| 図 | ず | **ちず** |
| 記 | きろく | **にっき** |
| 方 | ほうこう | **ほうがく** |
| 社 | やしろ | **かいしゃ** |
| 絵 | えをかく | **え** |

---

## 4. 1学期ぶんの収録内容（40問）

| ページ | 漢字（列順） |
|---|---|
| 1 | 書・図・南・行・言・声・雪・読 |
| 2 | 肉・曜・記・思・春・知・絵・方 |
| 3 | 風・高・毛・**太**・黒・黄・聞・話 |
| 4 | 同・近・長・体・形・考・新・多 |
| 5 | 姉・店・内・切・刀・社・会・今 |

読み一覧：かく／ちず／みなみ／いく／いう／こえ／ゆき／よむ／にく／ようび／にっき／おもう／はる／しる／え／ほうがく／かぜ／たかい／け／**ふとい**／くろ／きいろ／きく／はなす／おなじ／ちかい／ながい／からだ／かたち／かんがえる／あたらしい／おおい／あね／みせ／うち／きる／かたな／かいしゃ／あう／いま

---

## 5. ハマりどころ（次回も起きる可能性が高い）★必読

| 症状 | 原因 | 対処 |
|---|---|---|
| **列ズレ**（別の字が混ざる） | ページ全体で列を切った。行ごとに文字位置がズレている | 行ごとに8文字を個別検出する |
| ある行だけ9個検出される | 偏と旁が離れた字（行 など）が2つに分裂 | 統合ギャップを可変にし、8個になる値を採用 |
| 余計な行・列が検出される | ページ端に**指が写り込んでいる** | 高さ・幅の中央値比でフィルタ |
| 字にマス線が残る | 罫線が文字と接触して1つの塊になる | 細い直線だけを狙って除去（太さで判別） |
| ある行だけ字が薄い | 筆圧の差 | 行ごとにインク量を正規化 |
| **読みの取り違え** | Claudeが手書きを誤読（例：太→犬と誤認） | **必ずユーザーが確認シートで検品する**（下記） |

### 検品のやり方（重要）
Claudeは処理後に **ページごとの確認シート**（8列×4行、正解に緑枠）を出力する。
ユーザーはこれを見て「読みが漢字と合っているか」だけチェックすればよい。
※ 正解位置（4行目）は機械的に決まるので確認不要。**誤読リスクがあるのは"読み"だけ。**

---

## 6. 次の学期のやり方（ユーザー向け・3ステップ）

1. 同じ形式でノートに書く（1列＝1問、**上から4つ目が正解**、1ページ8列）
2. 写真を撮ってPDFにし、`kanjimon/` に置いてClaudeに渡す
3. Claudeが出す確認シートを見て、**読みの間違いだけ指摘**する

→ Claudeが画像・CSV作成、commit & push、GitHub Pagesへの公開、ゲームでの読み込みテストまで実施。

ファイル名の規則（例：2学期なら）
- CSV: `questions_grade2_term2.csv`
- 画像: `images/quiz/` に同じ命名規則で追加（ページ番号が重複する場合は接頭辞を変える）

---

## 7. 検証済みの動作（1学期ぶん）

- CSV読み込み：「✅ 40もん よみこみました！」
- 正解判定：10問連続テストで全て `_4.png` が正解と一致
- 画像表示：スライムの看板に手書き漢字が正しく表示
- コンソールエラー：なし
- 公開URL：CSV・画像とも配信確認済み

---

## 補足：関連ドキュメント
- `HANDOFF_3D.md` … このゲームを3D化する際の引き継ぎ資料（ゲーム仕様・35体ロースター・ボス等）
- `README.md` … ゲーム本体の説明
